<!DOCTYPE html>
<html>

<head>
    <title>Users</title>
    <meta charset="utf-8" />
</head>

<body>
    <h1>Users</h1>
        
    <table>
        <tr>
            <th>Name</th>
            <th>Age</th>
            <th></th>
        </tr>
        {{#each users}}
        <tr>
            <td>{{this.name}}</td>
            <td>{{this.age}}</td>
            <td>
                <a type="button" href="/users/{{this.id}}">Edit</a>|
                <form action="/users/{{this.id}}/delete" method="POST" style="display:inline;">
                    <input type="submit" value="Delete" />
                </form>
            </td>
        </tr>
        {{/each}}
    </table>

     <a href="/">Start page</a>
</body>
<html>